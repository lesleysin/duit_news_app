import { ComponentDescription, SizedBox } from "duit_js";

export const SeparatorComponent = ComponentDescription("separator", SizedBox({attributes: {width: 8}}))
export const SeparatorVerticalComponent = ComponentDescription("separator_vertical", SizedBox({attributes: {height: 12}}))